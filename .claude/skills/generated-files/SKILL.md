---
name: generated-files
description: lib/api.lua as the single source of the player-facing API, and the seven things in the CodeBlock mod that restate the source and drift in silence — doc/api.md, locale/template.txt, settingtypes.txt, .luacheckrc's sandbox std, .cdb.json, the new-file template and the example-name list. Which are generated, which are checked, which are neither, and the rules for a new S() key.
when_to_use: Before editing lib/api.lua, scripts/gen_docs.lua, scripts/gen_locale.lua, scripts/gen_settingtypes.lua, scripts/gen_cdb_json.sh, .luacheckrc or CONTENTDB.md. Also when changing a player-facing name, adding a limit, adding or editing an S() string, adding a file to the tree, or writing a check that must be able to fail.
allowed-tools: Read, Grep, Glob, Bash, Edit, Write
---

# The API has one source

**`lib/api.lua` is pure data and the single description of everything a program
can call.** Three things derive from it: the sandbox environment, the in-game
help panel (`api.to_hypertext`) and `doc/api.md` (`api.to_markdown`).

**`api.build` raises if description and implementations disagree in either
direction**, so the mod refuses to load rather than ship a reference that lies.

**Changing a player-facing name spans four files**: `lib/api.lua`, the `impls`
table in `lib/sandbox.lua`, `stds.codeblock_sandbox` in `.luacheckrc` and the
explicit name list in `tests/api_spec.lua` — then regenerate `doc/api.md`. **It
breaks saved player programs**, which are data no game can migrate, so it is a
major version bump.

**`doc/api.md` is generated only from the `# Lua api` heading onward.** What sits
above it — `# Codelevel` and `# Chat commands` — is hand-written and read by
nothing. The gate can report *doc/api.md is up to date* over a chat command that
no longer exists, which is what it did while `F10` renamed `/codelevel` and
`/codegenerate`. **Edit that region by hand when a command or a codelevel limit
changes, and do not read a green `--check` as covering it.** The blind spot is by
design: that region describes chat commands and privileges, which `lib/api.lua`
knows nothing about.

# The seven restatements of the source

Each is read by a human, a linter, ContentDB or a player rather than by the code,
so **nothing fails when one is wrong**.

| Restates | File | Status |
|---|---|---|
| `lib/api.lua` | `doc/api.md` | generated, `gen_docs.lua --check` |
| the code's `S()` keys | `locale/template.txt` | generated, `gen_locale.lua --check` (`C17`) |
| `lib/config.lua` | `settingtypes.txt` | generated, `gen_settingtypes.lua --check` |
| `api.names()` | `.luacheckrc` `stds.codeblock_sandbox` | checked both ways by `gen_docs.lua` |
| `CONTENTDB.md` | `.cdb.json` | generated by `gen_cdb_json.sh`, verified by nothing |
| the API | the new-file template in `lib/formspecs.lua` | checked by `tests/integration_spec.lua` |
| `lib/examples/` | the name list in `tests/preprocess_spec.lua` | half-checked (`C23`, open) |

**The pattern: a note about remembering does not hold, a `--check` in CI does.**

**A generator guarantees the output matches its input, and nothing more.**
`.cdb.json` never drifted — for the project's whole life it was faithfully
generated from the wrong source, `README.md`, breaking six ContentDB rules at
once (`C19`). The source is now `CONTENTDB.md`; edit that and run
`bash scripts/gen_cdb_json.sh`, never `.cdb.json`. Its recent-changes list
against `CHANGELOG.md` is hand-kept and nothing checks the two agree.

**`settingtypes.txt` only draws the settings menu** — the engine reads no
defaults from it. `gen_settingtypes.lua` derives the **numbers** from
`lib/config.lua` and **owns the words**: `config.lua`'s comments are for whoever
edits the code and the menu's descriptions are for an administrator, so it
guarantees the defaults agree and nothing about the prose. Beyond that it checks
**completeness in both directions**: a setting `config.lua` reads that the menu
does not offer, or a menu entry nothing reads, fails by name.

**Both `--check` scripts run under a bare Lua 5.1 with no engine global.**
`gen_locale.lua` also lists `lib/` through `ls`, so it wants a POSIX shell rather
than a hand-kept list of its own inputs — that being the same defect one level
up.

**A file added to the tree needs a `.gitattributes` line or it ships to a
player.** ContentDB builds with `git archive` and no CI checks it. `AUDIT.md` and
`PLAYTEST.md` each carry their own `export-ignore` line.

## `.luacheckrc`'s std is checked, not generated

**The interesting difference**: the file is a linter configuration a human also
edits, and generating it would take it away from whoever has to change it.

**It lints `lib/examples/**`, so a name missing from it inverts what it exists
for.** An example calling a correct API name is reported `(W113) accessing
undefined variable`, which reads as a typo and invites someone to edit working
player code. `sleep` and `default_block` were missing for a year (`C22`).

**`gen_docs.lua --check` compares that list with `api.names()` in both
directions**, by `loadfile`+`setfenv` on the config rather than by pattern
matching, so it reads a nested `ramp = {fields = {...}}` correctly. It runs in
write mode too, so the generator refuses to write `doc/api.md` while the two
disagree. Two rules follow:

- **The std holds API names and nothing else.** That is what makes the comparison
  a plain equality instead of an equality with an exemption list. Anything else —
  the bare `_` the examples pass for *use the default for this argument* — goes in
  `files["lib/examples/**"].read_globals`, which luacheck adds to the std.
- **A bare string entry is a silent escape hatch.** luacheck accepts *every*
  field of a bare-string name, which is what `vector` needs, so replacing
  `ramp = {fields = {...}}` with a plain `"ramp"` passes the check and switches
  off typo-catching for `ramp.*` with nothing going red. Spell out the fields of
  a name whose fields are all described — `random = {fields = {"of", "hues"}}`.
- **Deleting the last dotted name under a prefix deletes the prefix.** The
  comparison runs both ways, so `table` had to come out of the std the moment
  `table.randomizer` came out of `lib/api.lua` (`F17`) — otherwise
  `stds.codeblock_sandbox lists what lib/api.lua does not describe: table`.
  Check no example uses the prefix before removing it; none did.

## The new-file template

**The program `create_file` in `lib/formspecs.lua` writes into a new file is
player code inside a Lua string literal.** Nothing lints, compiles or generates
it. It said `place(blocks.obsidian)` for three days after `F11` deleted the
`blocks` category: every file a player created failed on its first statement,
with all five gates green (`B53`).

**`tests/integration_spec.lua` reads the string out of `lib/formspecs.lua`** —
anchored on the `write_file(name, filename,` call — and runs it through
`get_safe_coroutine`, with the pre-`F11` text kept as a control that must fail. A
copy of the template in the spec would be one more mirror.

**The template names no individual colour**:
`for i = 1, #hues do place(hues[i]) up(1) end`, because `place`, `up` and `hues`
change only in a major version while a colour name is the part of the API that
moves.

## The example-name list, still half-checked

**`tests/preprocess_spec.lua` is the only thing that compiles the shipped
examples**, and it checks them against a hand-written list rather than against
`lib/examples/`. A listed name with no file fails by name; **a file with no
listed name is compiled by nothing** (`C23`, open). The real set is
`codeblock.examples.examples`, built at load from `core.get_dir_list`. The fix is
blocked only by an untracked example in that directory making the check red
locally and green in CI.

# A check that cannot fail is indistinguishable from one that passes

**Make a new check fail once before trusting it.** Two here were written,
committed, believed and matched nothing: `gen_docs.lua`'s documented-limit guard
first listed three name prefixes that missed every limit being added, and its
replacement matched by shape with `%w+` — and **Lua's `%w` excludes the
underscore that every limit name contains**, so it matched nothing from the day
it was written (`C20`). Both generators are now `[%w_]+`, and both have been run
against a deliberately undrawn limit and seen to exit 1.

# Three rules for a string a player sees

- **Never build a translation key with `..`.** The literal argument to `S()` *is*
  the key, so a concatenated one cannot be extracted, cannot be translated, and
  nothing reports it.
- **Never edit a key in the source alone.** A trailing space, a plural or a
  capital orphans the existing translation with no error anywhere, and it still
  looks translated in the `.tr` file. Only a diff of the two key lists sees it.
- **A new `S()` key ships English-by-default and nothing fails.**
  `gen_locale.lua --check` fails on `locale/template.txt` only; an incomplete
  `.tr` it merely *reports*, which is correct, because an untranslated message
  legitimately falls back to English. **A feature adding player-facing strings is
  not finished until the `.tr` files are written**, and the only thing that will
  tell you is playing it in another language. Read the check's *untranslated*
  list, not just its exit line.

## Related

- **`program-pipeline`** — why `lib/config.lua`'s limit tables must stay plain
  literals for the two generators to see them.
- **`blocks-and-palette`** — a top-level API name costing every game a category
  name.
- **`run-tests`** — running the gates and reading their output.
